#include <stdio.h>

int main(int argc, char const *argv[])
{
    int x, y, i, n, p;

    if (x < 0) {
        printf("negative");
    }

    while (i >= 0)
    {
        printf("%d\n", i);
        i--;
    }

    // for (inicialização; teste; atualização)
    // {
    //     /* code */
    // }
    
    for (i = 0; i < n; i++)
    {
        /* code */
    }

    for (p = 1; p<=512; p*= 2) //exibir a potencia de 2 até 512 com um laço for

    {
        printf("%d\n", p);
    }
    
    
    
    return 0;
}
